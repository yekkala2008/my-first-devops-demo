name: My First Pipeline

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello, DevOps World!"

      - name: Simulate Work
        run: echo "Running some checks..."
